stages:
  - setup
  - test

setup:
  stage: setup
  script:
    - npm install
    - npm run init
    
test:
  stage: test
  script:
    - npm test

cache:
  untracked: true
  key: "$CI_BUILD_REF_NAME"
  paths:
    - node_modules/
